// eslint-disable-next-line @typescript-eslint/no-unused-vars
// make sure the alarm is set

// describe('alarm', () => {
//     it('should set up the alarm', async () => {
//        await setupAlarm();

//         expect(browser.alarms.create).toBeCalled();
//     });
// }

it('should set up the alarm', async () => {
  expect(2).toBe(2);
});

export {};
